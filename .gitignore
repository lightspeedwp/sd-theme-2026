# OS
.DS_Store
Thumbs.db

# Editor
.idea/
*.swp
*.swo

# Dependencies / build output (none shipped today — guard rails only)
node_modules/
vendor/
build/
dist/
*.log

# Local agent + tooling state
.claude/settings.local.json
.claude/*.local.json

# Never commit secrets or environment config
.mcp.json
.env
.env.*
wp-config.php

# Commercially-licensed font faces.
#
# Joe Hand IS committed, as of 2026-09-09. It is licensed for web embedding (JOEBOB graphics
# Webfont EULA 1.0, licence owner Southern Destinations, LightSpeed acting as its agent) and
# the §1.3 pageview cap is accepted by the client. It was previously ignored on a
# redistribution reading of §1.5/§1.6 — but nothing placed it, so it 404'd on dev and would
# have 404'd on production at launch. Committing it is the decision that makes the face
# actually work; this repository is private and access is agency + client only.
#
# ⚠️ That decision depends on the repo staying private. If it is ever made public, this face
# must come out and history must be rewritten first. See CHANGELOG.md → Security.
#
# Optima is still ignored, and for a different reason: no web-licensed file exists yet. The
# web licence covers *Optima DemiBold* alone and is still being purchased (SD, 2026-09-07).
# Twelve DESKTOP OTFs sit in the workspace for the client's Canva use, and their EULA forbids
# server installation and defines OTF→WOFF2 conversion as a Derivative Work — so this pattern
# guards a plausible accident, not a theoretical one. Revisit it when the kit arrives; the
# same reasoning that committed Joe Hand will probably apply. → LS-2641
#
# The OFL/Apache faces (Open Sans, Belleza, La Belle Aurore) are committed and safe to ship.
assets/fonts/optima-*.woff2

# Test artefacts
tests/**/test-results/
tests/**/playwright-report/
tests/**/node_modules/

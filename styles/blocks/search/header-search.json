{
	"$schema": "https://schemas.wp.org/wp/6.9/theme.json",
	"version": 3,
	"title": "Header Search",
	"slug": "header-search",
	"blockTypes": [
		"core/search"
	],
	"description": "The header's fold-out search — live's `#masthead #searchform`, which is already a disclosure: the `.search-field` sits at `width: 20px; opacity: 0` behind a 20px magnifier and unrolls on a `0.4s ease-in-out` transition. Measured on live 2026-08-20. Replaces the `.sd-header__search` class the treatment used to be configured by, so it is now chosen from the block's Styles panel like every other style in this theme.\n\n## No JavaScript, and none needed\n\nOn WordPress 7.0 `core/search` is expandable natively whenever `buttonPosition` is `button-only`: wp-includes/blocks/search.php enqueues the Interactivity view module, binds `aria-expanded` and `aria-label` on the button, and toggles `wp-block-search__searchfield-hidden` **on the form** via `data-wp-class`. Escape and focus-out close it. The disclosure, its ARIA and its keyboard behaviour are all core's; the theme only draws it. (`isSearchFieldHidden` is a pre-Interactivity leftover, no longer in core's block.json — don't set it.)\n\n## What is here, and what is in CSS\n\nHere: the trigger's design. `core/search` runs its button through `wp_theme_get_element_class_name('button')`, so it is a real `.wp-element-button` and would otherwise inherit theme.json's brand-500 ground, padding and radius and render as a large orange tile. Structured `elements.button` rather than a `css` field so the editor renders it and so the field's `:hover` strip doesn't apply.\n\nThe fold-out mechanism — the panel's geometry and its open/closed states, the icon sizing, the reduced-motion opt-out — is in assets/styles/core-search.css, scoped to `.is-style-header-search`. It is long, stateful, and one part of it (`@media`) cannot compile in a `css` field at all. A `css` field big enough to hold all of it is a JSON string nobody can read or diff, which is how this drifted the first time.\n\nBorder is deliberately **not** a structured property on the panel either: `core/search`'s `selectors.border` covers `.wp-block-search__button-only .wp-block-search__input` *and* `… .wp-block-search__button`, so a structured border would draw a box around the bare magnifier as well as the panel.\n\n## css field limits, measured on WP 7.0, 2026-08-21\n\nFor whoever adds to this later. Survives: `&`, `&.class`, `&:not(.class)`, `& .a`, `& .a .b`, commas inside declaration values (`min()`, `cubic-bezier()`, multi-part `transition`), and structured `elements.button` with `:hover`/`:focus`. Does **not** survive: `@media` — it compiles to a dead `:root :where(.is-style-header-search--N@media (max-width: 960px)){}` plus the inner rule emitted unconditionally at every width, which is silently wrong rather than silently absent. The theme's skill reference claimed `@media` survives; it does not, and the reference has been corrected.",
	"styles": {
		"elements": {
			"button": {
				"color": {
					"background": "transparent",
					"text": "var:preset|color|neutral-800"
				},
				"border": {
					"width": "0",
					"radius": "0"
				},
				"spacing": {
					"padding": {
						"top": "var:preset|spacing|5",
						"right": "var:preset|spacing|5",
						"bottom": "var:preset|spacing|5",
						"left": "var:preset|spacing|5"
					}
				},
				":hover": {
					"color": {
						"background": "transparent",
						"text": "var:preset|color|brand-500"
					}
				},
				":focus": {
					"color": {
						"background": "transparent",
						"text": "var:preset|color|brand-500"
					}
				}
			}
		}
	}
}

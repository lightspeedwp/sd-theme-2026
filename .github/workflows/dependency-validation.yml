name: dependency validation

# Untrusted stage. Runs pull-request code inside a disposable one-job container
# on the dependency-ci-ephemeral group. Holds no merge, enrolment or
# runner-manager credential. Never uses the persistent trusted merge runner.
#
# Deliberately toolchain-free: the container ships git/curl/jq only. Do NOT add
# shivammathur/setup-php here - its self-hosted locking calls sudo, which is what
# caused the 2026-08-03 security-mail incident. Anything needing PHP, Composer or
# npm must bring its own pinned static tooling.
#
# GitHub's own Dependabot cannot be routed to our self-hosted runners - a PR
# IT authored gets this workflow's job queued indefinitely on a self-hosted
# runner unless the org enables "Dependabot on self-hosted runners" org-wide,
# which is deliberately off (that setting also hands Dependabot's own
# internal update job access to self-hosted infra - a materially bigger
# trust boundary than validating one PR). So a Dependabot-authored PR runs
# a hosted copy of this job instead; every other PR runs the self-hosted one.
#
# Split into two statically-routed jobs (validate / validate-dependabot),
# NOT one job with a computed `runs-on` fed by a `choose-runner` job's
# output. A managed repo's own CI-safety lint has to prove a job's runner
# selector is safe by reading the YAML alone, with no live GitHub context -
# `runs-on: ${{ fromJson(needs.x.outputs.y) }}` is exactly the pattern that
# defeats that (verified live on lightspeed-hosting-infra: its lint fails
# closed on any runner expression it cannot resolve to a literal, and that
# fail-closed check does not consult the same exception list a resolved-hosted
# verdict would - so no exception entry there can rescue a dynamic expression,
# only a literal `runs-on` can). Both jobs below duplicate their steps rather
# than share them through a computed runner, so each one's `runs-on` is a
# plain string or array literal a downstream repo's static check can classify.
#
# Managed centrally by lightspeedwp/dependency-merge-controller.

on:
  pull_request:
    branches: [main, master, develop]

permissions:
  contents: read

concurrency:
  group: dependency-validation-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  validate:
    name: dependency-validation
    # Dependency pull requests only. Anything else is not this workflow's job.
    # Dependabot-authored PRs run the hosted copy below instead.
    if: |
      (startsWith(github.head_ref, 'dependabot/') ||
       startsWith(github.head_ref, 'deps/') ||
       startsWith(github.head_ref, 'renovate/')) &&
      github.event.pull_request.user.login != 'dependabot[bot]'
    runs-on: [self-hosted, linux, x64, dependency-ci-ephemeral]
    timeout-minutes: 20
    env:
      GITLEAKS_VERSION: '8.30.1'
      GITLEAKS_SHA256: '551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb'
      BASE_REF: ${{ github.base_ref }}
    steps:
      - name: Confirm disposable container
        run: |
          set -euo pipefail
          echo "container=$(hostname) user=$(id -un) uid=$(id -u)"
          # Fail closed if this is not the isolated container we expect.
          [ "$(id -u)" != "0" ] || { echo 'running as root - refusing'; exit 1; }
          [ ! -e /var/run/docker.sock ] || { echo 'docker socket present - refusing'; exit 1; }
          [ ! -d /var/www ] || { echo 'client sites visible - refusing'; exit 1; }

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Secret scan (pinned, checksum verified)
        run: |
          set -euo pipefail
          tmp="$(mktemp -d)"
          trap 'rm -rf "${tmp}"' EXIT
          tarball="gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz"
          curl --fail --silent --show-error --location --retry 3 \
            -o "${tmp}/${tarball}" \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/${tarball}"
          echo "${GITLEAKS_SHA256}  ${tmp}/${tarball}" | sha256sum --check --status
          tar -xzf "${tmp}/${tarball}" -C "${tmp}" gitleaks
          if [ -f .gitleaks.toml ]; then
            "${tmp}/gitleaks" dir . --config .gitleaks.toml --redact --no-banner
          else
            "${tmp}/gitleaks" dir . --redact --no-banner
          fi

      - name: Dependency manifest sanity
        run: |
          set -euo pipefail
          # A lockfile change with no manifest change, or vice versa, is a
          # signal the trusted stage should not merge on. Report, do not judge.
          if [ -z "${BASE_REF}" ]; then
            echo "BASE_REF is empty - refusing to diff against an unbounded ref" >&2
            exit 1
          fi
          changed="$(git diff --name-only "origin/${BASE_REF}"...HEAD || true)"
          echo "changed files:"; printf '%s\n' "${changed}"
          for pair in "composer.json:composer.lock" "package.json:package-lock.json"; do
            m="${pair%%:*}"; l="${pair##*:}"
            if printf '%s\n' "${changed}" | grep -qx "${l}" \
               && ! printf '%s\n' "${changed}" | grep -qx "${m}"; then
              echo "note: ${l} changed without ${m}"
            fi
          done
          echo "validation complete"

  validate-dependabot:
    name: dependency-validation (dependabot, hosted)
    # Dependabot cannot reach our self-hosted runners (see the top-of-file
    # comment). This is the ONLY job in this workflow that may run on a
    # GitHub-hosted runner - a downstream repo recording an exception for
    # it is recording exactly this job, not a general "hosted is fine".
    if: |
      (startsWith(github.head_ref, 'dependabot/') ||
       startsWith(github.head_ref, 'deps/') ||
       startsWith(github.head_ref, 'renovate/')) &&
      github.event.pull_request.user.login == 'dependabot[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      GITLEAKS_VERSION: '8.30.1'
      GITLEAKS_SHA256: '551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb'
      BASE_REF: ${{ github.base_ref }}
    steps:
      # No "Confirm disposable container" step here: a GitHub-hosted runner
      # genuinely has a docker socket and runs as a non-root "runner" user
      # by design, so that check would misfire on this job every time.

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Secret scan (pinned, checksum verified)
        run: |
          set -euo pipefail
          tmp="$(mktemp -d)"
          trap 'rm -rf "${tmp}"' EXIT
          tarball="gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz"
          curl --fail --silent --show-error --location --retry 3 \
            -o "${tmp}/${tarball}" \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/${tarball}"
          echo "${GITLEAKS_SHA256}  ${tmp}/${tarball}" | sha256sum --check --status
          tar -xzf "${tmp}/${tarball}" -C "${tmp}" gitleaks
          if [ -f .gitleaks.toml ]; then
            "${tmp}/gitleaks" dir . --config .gitleaks.toml --redact --no-banner
          else
            "${tmp}/gitleaks" dir . --redact --no-banner
          fi

      - name: Dependency manifest sanity
        run: |
          set -euo pipefail
          # A lockfile change with no manifest change, or vice versa, is a
          # signal the trusted stage should not merge on. Report, do not judge.
          if [ -z "${BASE_REF}" ]; then
            echo "BASE_REF is empty - refusing to diff against an unbounded ref" >&2
            exit 1
          fi
          changed="$(git diff --name-only "origin/${BASE_REF}"...HEAD || true)"
          echo "changed files:"; printf '%s\n' "${changed}"
          for pair in "composer.json:composer.lock" "package.json:package-lock.json"; do
            m="${pair%%:*}"; l="${pair##*:}"
            if printf '%s\n' "${changed}" | grep -qx "${l}" \
               && ! printf '%s\n' "${changed}" | grep -qx "${m}"; then
              echo "note: ${l} changed without ${m}"
            fi
          done
          echo "validation complete"
